<Window x:Class="LunaForge.Views.CreateProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:LunaForge.ViewModels"
        xmlns:tools="clr-namespace:LunaForge.Tools"
        mc:Ignorable="d"
        Title="New Project..."
        Height="450"
        Width="600"
        MinHeight="400"
        MinWidth="200"
        WindowStartupLocation="CenterScreen"
        Style="{DynamicResource LFWindow}">
    <Window.DataContext>
        <vm:CreateProjectViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <tools:Int32ToVisibilityConverter x:Key="Int32ToVisibilityConverter"/>
        <tools:InvertedBoolConverter x:Key="InvertedBoolConverter"/>
        <tools:ObjectEqualityConverter x:Key="ObjectEqualityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="25"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!--#region Details -->

                <TextBlock Text="Project name:"
                           Grid.Row="0"
                           FontSize="12"
                           FontWeight="Bold"/>
                <TextBox Text="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged}"
                         Grid.Row="1"
                         VerticalContentAlignment="Center"
                         Height="25"
                         FontSize="12"/>

                <TextBlock Text="Author:"
                           Grid.Row="0"
                           Grid.Column="2"
                           FontSize="12"
                           FontWeight="Bold"/>
                <TextBox Text="{Binding ProjectAuthor, UpdateSourceTrigger=PropertyChanged}"
                         Grid.Row="1"
                         Grid.Column="2"
                         VerticalContentAlignment="Center"
                         Height="25"
                         FontSize="12"/>

                <TextBlock Text="Location:"
                           Grid.Row="2"
                           Grid.Column="0"
                           Grid.ColumnSpan="3"
                           FontSize="12"
                           FontWeight="Bold"/>
                <Grid Grid.Row="3"
                      Grid.Column="0"
                      Grid.ColumnSpan="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             Text="{Binding ProjectPath, Mode=OneWay}"
                             Height="25"
                             FontSize="12"
                             Foreground="#666"
                             Background="#F5F5F5"
                             BorderThickness="1"
                             IsReadOnly="True"/>

                    <Button Grid.Column="1"
                            Content="Browse..."
                            Command="{Binding ChangePathCommand}"
                            Height="25"
                            Margin="5,0,0,0"/>
                </Grid>

                <TextBlock Text="LuaSTG Instance:"
                           Grid.Row="4"
                           FontSize="12"
                           FontWeight="Bold"/>
                <Grid Grid.Row="5"
                      Grid.Column="0"
                      Grid.ColumnSpan="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ComboBox Grid.Column="0"
                              ItemsSource="{Binding Instances}"
                              SelectedItem="{Binding SelectedInstance}"
                              Height="25"
                              FontSize="12"
                              VerticalContentAlignment="Center"/>

                    <Button Grid.Column="1"
                            Content="Manage..."
                            Command="{Binding OpenInstanceManagerCommand}"
                            Height="25"
                            Margin="5,0,0,0"/>
                </Grid>

                <!--#endregion Details -->
            </Grid>
            <Grid Grid.Column="1" Margin="5">
                <!--#region Templates -->
                <DockPanel>
                    <TextBlock Text="Select Project Template:"
                               FontSize="15"
                               FontWeight="Bold"
                               Margin="0,5"
                               DockPanel.Dock="Top"/>
                    <ListView ItemsSource="{Binding Templates}"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              HorizontalContentAlignment="Stretch"
                              VerticalContentAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              HorizontalAlignment="Stretch"
                              BorderBrush="Gray"
                              BorderThickness="1">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="5">
                                    <!--BorderThickness="1"
                                        BorderBrush="Gray"
                                        CornerRadius="3"-->
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="15"
                                                   FontWeight="Bold"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,2"/>
                                        <TextBlock Text="{Binding Description}"
                                                   TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding DisplaySource}"
                                                   FontSize="10"
                                                   Foreground="Gray"
                                                   Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </DockPanel>
                <!--#endregion Templates -->
            </Grid>
        </Grid>
        <StackPanel Grid.Row="1" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Content="OK" Margin="2" Padding="5" Command="{Binding OkCommand}"/>
            <Button Content="Cancel" Margin="2" Padding="5" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
