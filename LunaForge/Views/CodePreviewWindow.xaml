<Window x:Class="LunaForge.Views.CodePreviewWindow"
        x:Name="Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LunaForge.Views"
        xmlns:controls="clr-namespace:LunaForge.Controls"
        mc:Ignorable="d"
        Title="Code Preview"
        Height="250"
        Width="400"
        Style="{DynamicResource LFWindow}">
    <Window.Resources>
        <FontFamily x:Key="JBMono">/LunaForge;component/#JetBrains Mono Regular</FontFamily>
    </Window.Resources>
    <Grid>
        <controls:LuaTextEditor
            Text="{Binding CodeText, ElementName=Window}"
            FontFamily="{StaticResource JBMono}"
            FontSize="12"
            ShowLineNumbers="True"
            WordWrap="False"
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Auto"
            IsReadOnly="True"/>
    </Grid>
</Window>
