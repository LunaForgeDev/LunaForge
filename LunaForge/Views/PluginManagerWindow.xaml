<Window x:Class="LunaForge.Views.PluginManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:LunaForge.ViewModels"
        xmlns:tools="clr-namespace:LunaForge.Tools"
        mc:Ignorable="d"
        Title="Plugin Manager"
        Height="500"
        Width="700"
        MinHeight="400"
        MinWidth="600"
        WindowStartupLocation="CenterOwner"
        Style="{DynamicResource LFWindow}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <tools:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <tools:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <tools:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <tools:InvertedBoolConverter x:Key="InvertedBoolConverter"/>
    </Window.Resources>
    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Reload All Plugins" 
                    Command="{Binding ReloadPluginsCommand}"
                    IsEnabled="{Binding IsReloading, Converter={StaticResource InvertedBoolConverter}}"
                    Padding="10,5"
                    Margin="0,0,10,0"
                    MinWidth="120"/>
            <Button Content="Close" 
                    Command="{Binding CloseCommand}"
                    Padding="10,5"
                    MinWidth="80"/>
        </StackPanel>

        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <TextBlock DockPanel.Dock="Top" 
                           Text="Installed Plugins" 
                           FontWeight="Bold" 
                           Margin="0,0,0,5"/>
                <DataGrid ItemsSource="{Binding Plugins}"
                          SelectedItem="{Binding SelectedPlugin, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserResizeRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" 
                                            Binding="{Binding DisplayName}" 
                                            Width="*"/>
                        <DataGridTextColumn Header="Version" 
                                            Binding="{Binding Version}" 
                                            Width="80"/>
                        <DataGridTextColumn Header="Status" 
                                            Binding="{Binding StatusText}" 
                                            Width="80"/>
                        <DataGridTemplateColumn Header="Enabled" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsEnabled, Mode=OneWay}"
                                              IsEnabled="{Binding IsBuiltIn, Converter={StaticResource InvertedBoolConverter}}"
                                              Command="{Binding DataContext.TogglePluginCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                              CommandParameter="{Binding}"
                                              HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>

            <GridSplitter Grid.Column="1" 
                          HorizontalAlignment="Stretch" 
                          ShowsPreview="True" DragDelta="GridSplitter_DragDelta"
                          Opacity="0"/>

            <DockPanel Grid.Column="2">
                <TextBlock DockPanel.Dock="Top" 
                           Text="Plugin Details" 
                           FontWeight="Bold" 
                           Margin="0,0,0,10"/>
                <StackPanel DataContext="{Binding SelectedPlugin}">
                    <GroupBox Header="Information" 
                              Margin="0,0,0,10"
                              Visibility="{Binding Converter={StaticResource NullToVisibilityConverter}, FallbackValue=Collapsed}">
                        <StackPanel Margin="5">
                            <Label Content="Name:" FontWeight="Bold"/>
                            <TextBlock Text="{Binding DisplayName}" 
                                       Margin="5,0,0,10" 
                                       TextWrapping="Wrap"/>

                            <Label Content="Library Name:" FontWeight="Bold"/>
                            <TextBlock Text="{Binding LibraryName}" 
                                       Margin="5,0,0,10" 
                                       TextWrapping="Wrap"/>

                            <Label Content="Version:" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Version}" 
                                       Margin="5,0,0,10"/>

                            <Label Content="Status:" FontWeight="Bold"/>
                            <TextBlock Text="{Binding StatusText}" 
                                       Margin="5,0,0,10"/>

                            <Label Content="Categories:" FontWeight="Bold"/>
                            <TextBlock Text="{Binding CategoryCount}" 
                                       Margin="5,0,0,10"/>

                            <Label Content="Description:" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Description}" 
                                       TextWrapping="Wrap" 
                                       Margin="5,0,0,10"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Note" 
                              Visibility="{Binding IsBuiltIn, Converter={StaticResource BoolToVisibility}, FallbackValue=Collapsed}">
                        <TextBlock Text="This is a built-in plugin and cannot be disabled." 
                                   TextWrapping="Wrap" 
                                   Margin="5"/>
                    </GroupBox>

                    <TextBlock Text="Select a plugin to view details" 
                               Foreground="Gray" 
                               TextAlignment="Center"
                               VerticalAlignment="Center"
                               Margin="0,50,0,0"
                               Visibility="{Binding Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                </StackPanel>
            </DockPanel>
        </Grid>
    </DockPanel>
</Window>
